<!--  SECTION 1 -->
        <div class="container" id="section_1" >
            
            <div id="login-box">
              <%= image_tag("NC_logo.svg", :alt => "Next Capital Logo", :id => "logo") %>
              <h1>Bowling Jackpot</h1> 
              <h3>Ready to have fun? Join us:</h3> 

              <div id="form-login">
                <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                      
       
                        <%= f.email_field :email, autofocus: true, placeholder: "Email", class: "text-input" %>
                        <br>

                 

                        <%= f.password_field :password, autocomplete: "off", placeholder: "Password", class: "text-input" %>
                        <br>

                      <% if devise_mapping.rememberable? -%>
                        
                          <%= f.check_box :remember_me %>
                          <%= f.label :remember_me %>
                          <br>
                      <% end -%>

              
                        <%= f.submit "Log in", id: "login-button" %>
           
                    <% end %>
                    <%= render "devise/shared/links" %>
                </div>

              <!-- <form id="form-login">
                <input class="text-input" type="email" value="" name="user[email]" id="user_email" placeholder="Email">
                <br>
                <input class="text-input" autocomplete="off" type="password" name="user[password]" id="user_password" placeholder="Password">
                <br>
                <input type="checkbox" value="1" name="user[remember_me]" id="user_remember_me">
                <label for="user_remember_me">Remember me</label>
                <br>
                <input id="login-button"type="submit" name="commit" value="Log in">
                <br>
              </form>  
              <a id="sign-up" href="/users/sign_up">Sign up</a>
              <a id="password" href="/users/password/new">Forgot your password?</a> -->
            </div>


        </div>



